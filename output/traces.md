The figure (image block) is missing - there should be a figure at bbox=[305.2, 239.5, 502.2, 436.3]
"Natural-Language Agent Harnesses" (block 0) is classified as "authors" instead of "title" - it IS bold and the largest text. Let me check - it has size 13.0 and title_size would be 18.1 (the watermark block). So avg_size >= title_size * 0.85 = 13 >= 18.1 * 0.85 = 15.4 is False.
That's the threshold issue preventing the title classification. The authors block (Linyue Pan...) is also misclassified as "paragraph" instead of "authors". And the figure block is completely missing from the output - I'm counting 14 items when there should be 15 blocks total, so the image block isn't being extracted at all even though the type checking logic should catch it.

Looking at the block inspection output, the image is there at index 9 with type information, but it seems the block type value might not be what I expected from fitz.

Let me trace through the label_abstract_blocks function more carefully - I'm checking which blocks fall between the abstract section header at index 4 and the introduction header at index 6, and I notice the image block at index 9 should definitely be classified as a figure, yet it's missing from the final JSON output entirely. I see the issue now—the figure block is being caught by the type check for figures, so it should be classified correctly. Let me trace through the actual logic more carefully to understand why it's not appearing in the output. The image block at index 9 should be classified as "figure" and included in the results, but it's missing from the JSON output—only 14 items instead of 15. Let me trace through the logic: get_text("dict") should include image blocks by default, but the TEXT_PRESERVE_WHITESPACE flag might be filtering them out, or there could be something specific about how fitz v1.27 handles image blocks in this context. I'm wondering if TEXT_PRESERVE_WHITESPACE is actually filtering out image blocks, or if the real issue is simpler—maybe I just need to call get_text("dict") without any flags, or combine the text blocks with image data from page.get_images() separately.

Let me focus on the actual problems I can see: the missing figure block, the title being misclassified as authors because the size threshold is too high (the watermark at 18.1 is skewing things when the real title is only 13.0), and the author line getting labeled as a paragraph. I should probably exclude sidebar or watermark text when calculating the title size threshold, or use a different heuristic altogether. The authors classification should be working based on these conditions, but it's being marked as a paragraph instead. Let me trace through the logic to see where the condition order might be causing issues with the title check interfering. I'm checking the classification logic more carefully now. Block 1 should pass the authors check since it meets all the criteria: the vertical position is in range, it's bold, the size is within bounds, and it has enough words. Yet the JSON output shows it classified as "paragraph" instead of "authors" — there's a mismatch between what the logic should produce and what's actually happening. I'm calculating the body size by finding the most common font size across all blocks—10.8 appears multiple times and rounds to 11. Now checking block 0 against the classification rules: it meets all the title criteria except the size threshold is too low (13.0 vs the required 15.4), so it incorrectly matches the authors condition instead. Block 1 also starts meeting the authors criteria with its size just above the body threshold. So the sizes round to the nearest integer, making 11 the most common value from the 10.8 spans, which means body_size is 11. For block 1, the authors condition would be true since 10.8 >= 10.45, but the JSON shows it classified as "paragraph" instead—so I need to check the actual order of conditions in the classify_block function to see what's happening.

Looking at the condition checks in sequence: after checking type, text content, and computing the span metrics like average size and bold status, the function checks watermark, table membership, page number, footnote, footer, title, authors, affiliation (both keyword and email variants), abstract, and section headers. For block 1 specifically, I can see it's marked as bold=True in the original inspection, so I need to trace through why the authors classification isn't triggering.

The bold flag check uses bit 4 (value 16) in PyMuPDF, which matches what I'm doing with f & 16. But looking at the actual text "Linyue Pan 1 Lexiao Zou..." - this should satisfy the authors condition, so I'm trying to understand why the JSON output shows it as a paragraph instead. I'm checking whether the conditions actually match for this block. The word count is definitely above 3, and the size constraints are working out — the average size of 10.8 falls right between the body size threshold and the title size limit, so those checks pass too.
